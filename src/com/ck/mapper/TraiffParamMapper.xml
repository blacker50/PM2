<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间-->
<mapper namespace="com.ck.mapper.TraiffParamMapper">
    <!--采用* 表的属性必须和pojos属性名相同-->
    <select id="selectTraiffParam" resultType="traiffParam">
        SELECT * FROM traiff_param where id=1
    </select>

</mapper>